# API base URL — update this when a real backend is ready
# NUXT_PUBLIC_API_BASE=https://your-api-domain.com

# Using mock data for now (set to "false" when real API is ready)
NUXT_PUBLIC_USE_MOCK_DATA=true

# Public site URL (used for OG images with nuxt generate)
# NUXT_PUBLIC_SITE_URL=
