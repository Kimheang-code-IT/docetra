{
  "framework": "nuxtjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.30.3 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "env": {
    "NUXT_PUBLIC_USE_MOCK_DATA": "true"
  }
}
